# OSV-Scanner ignore list. Each entry is an advisory that is unreachable from scankit's
# code AND has no fixed version available, so it cannot be resolved by upgrading. Reviewed
# manually; revisit if a fix ships.

[[IgnoredVulns]]
id = "GO-2026-5932"
reason = "golang.org/x/crypto/openpgp is unmaintained and flagged with no fix (Fixed in: N/A). scankit only uses the OPA Rego engine and never imports openpgp; govulncheck confirms the code does not call it. Pulled transitively via open-policy-agent/opa."
